version: '3.8'
services:
  mongodb:
    image: mongo:latest
    container_name: quizmaster-db
    ports:
      - "27018:27017"
    volumes:
      - mongodb_data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=quizmaster

volumes:
  mongodb_data:
